# Continuation (File Contract: active node state only)

This file states the current active node, why it is current, and what must be true to settle it and advance.

## Current active node
- **Node:** N11c

## Why this is current
- N11 is already settled on the roadmap criteria: camera grammar controls viewpoint, refinement, and semantic-band reveal over the existing one-object carrier, and recorded live review confirmed the QCD Bogo-Indian A initial-position reproduction no longer jumps when view distance crosses 4.6 -> 4.7 at neighborhood radius 4.
- Commit 92b1e3b reset the frontier from N12 back to N11a/N11b because the old anchored-entrypoint premise depended on phase-derived fixture bootstrap data instead of explicit opening-table, middlegame-procedural, and endgame-table regime surfaces.
- Commit 1e434e4 settled N11a by recording the unified regime substrate reset and opening N11b.
- N11b is now settled on the roadmap criteria: the planning artifacts explicitly map existing builder files, existing viewer files, and planned module families onto N11c through N11f, and they confirm that no further prerequisite nodes need to be inserted before the shared-contract work begins.
- The codebase still lacks versioned regime declarations, coverage metadata, provenance, and resolver inputs in the shared builder/runtime contract, so N11c is now the earliest unresolved node.
- The present builder/runtime seam still runs through `tools/builder/src/chessviz_builder/contracts.py`, `tools/builder/src/chessviz_builder/artifact_manifest.py`, `apps/viewer/src/viewer/contracts.ts`, `apps/viewer/src/viewer/navigation.ts`, and `apps/viewer/src/viewer/fixtureArtifacts.ts`; N11c is the point where those surfaces stop being phase/fixture-shaped and become regime-shaped.
- N11d through N11f are now scoped cleanly behind N11c: import-export publication follows the contract, resolver/bootstrap work follows the published surfaces, and fixture demotion follows the new loader path.

## Settle-and-advance conditions
- Builder shared-contract surfaces define versioned regime declarations and shared representation records for occurrence, transition, anchor, salience, provenance, coverage metadata, resolver inputs, and stable identity semantics.
- Viewer schema mirrors are updated to the same contract so runtime code consumes declared regime surfaces instead of inferring regime semantics from phase labels.
- The contract explicitly distinguishes annotation fields such as phase/material labels from regime-selection fields and provenance.
- N11c remains separable from table import-export work, resolver/bootstrap work, and fixture demotion even though it sets the contract they consume.
- A commit records N11c settlement and updates both `plan/completion-log.md` and this file to N11d or the next prerequisite frontier.

## Advancement rule
- Formal frontier events are tracked in `plan/completion-log.md` against the commit that records the exact continuation state transition.
